flask>=3.0
# Upper bound is load-bearing: data.py's reset_yf_for_process() reaches into
# yfinance.data.SingletonMeta._instances/._lock and YfData._cookie_lock to
# rebuild the singleton after gunicorn's --preload fork. There is no public
# equivalent, so a major release moving them would reinstate a worker
# crash-loop. tests/test_import_graph.py asserts those attributes exist.
yfinance>=1.7,<2
pandas>=2.0
matplotlib>=3.8
seaborn>=0.13
requests>=2.31
google-genai>=1.0
google-auth>=2.0
python-dotenv>=1.0
boto3>=1.34
prophet>=1.1
pmdarima>=2.0
statsmodels>=0.14
numpy>=1.26
xlrd>=2.0.1
openpyxl>=3.1
reportlab>=4.0
pdfplumber>=0.11
# No [pil] extra: ystocker/qr.py only calls QRCode.get_matrix(), a plain
# boolean grid, and rasterises it by hand -- pulling in Pillow just for that
# would cost every deploy a real dependency for a PNG encoder this repo
# already writes itself.
qrcode>=7.4
